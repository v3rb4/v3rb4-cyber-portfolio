<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-900">
  <head>
    <title><%= content_for(:title) || "V3rb4 Security Research" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Security Research and Penetration Testing">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  
  <body class="h-full text-gray-100">
    <!-- Minimal Side Navigation -->
    <nav class="fixed top-0 left-0 h-full w-16 md:w-64 bg-black bg-opacity-50 backdrop-blur-sm">
      <div class="flex flex-col h-full p-4">
        <!-- Logo -->
        <div class="mb-8">
          <%= link_to root_path, class: "flex items-center space-x-3" do %>
            <%= image_tag "cyber_avatar.png", alt: "Logo", class: "w-8 h-8" %>
            <span class="hidden md:block text-lg font-bold">v3rb4</span>
          <% end %>
        </div>

        <!-- Navigation Links -->
        <div class="space-y-6">
          <%= link_to root_path, class: "flex items-center space-x-3 text-gray-400 hover:text-white transition-colors" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
            </svg>
            <span class="hidden md:block">Home</span>
          <% end %>

          <%= link_to about_path, class: "flex items-center space-x-3 text-gray-400 hover:text-white transition-colors" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
            </svg>
            <span class="hidden md:block">About</span>
          <% end %>

          <%= link_to projects_path, class: "flex items-center space-x-3 text-gray-400 hover:text-white transition-colors" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
            </svg>
            <span class="hidden md:block">Projects</span>
          <% end %>

          <%= link_to contact_path, class: "flex items-center space-x-3 text-gray-400 hover:text-white transition-colors" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            <span class="hidden md:block">Contact</span>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <main class="ml-16 md:ml-64 min-h-screen">
      <%= yield %>
    </main>
  </body>
</html>
